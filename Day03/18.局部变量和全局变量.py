'''---------------------- 局部变量 ----------------------'''
# 全局变量
y = 50
def sum(a,b):# 函数参数
    # 定义在函数内部,局部变量
    result = a+b
    # 修改y变量,修改成70
    # 并不是修改全局变量,定义了一个局部变量y,值70
    # y = 70
    # 使用全局变量y
    global y
    y = 70
    print(y)
    return result


m = sum(10,20)
print(y)
print(m)